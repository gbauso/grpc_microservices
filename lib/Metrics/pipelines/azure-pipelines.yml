trigger:
  branches:
    include:
    - develop
    - master
  paths: 
    include:
    - lib/Metrics/*

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NuGetAuthenticate@0
  inputs:
    nuGetServiceConnections: 'nuget'
- task: DotNetCoreCLI@2
  inputs:
    command: 'build'
    projects: 'lib/Metrics/src/Metrics.csproj'
- task: NuGetCommand@2
  inputs:
    command: 'pack'
    packagesToPack: 'lib/Metrics/src/Metrics.csproj'
    versioningScheme: 'byPrereleaseNumber'
    majorVersion: '1'
    minorVersion: '0'
    patchVersion: '0'
- task: NuGetCommand@2
  inputs:
    command: 'push'
    packagesToPush: '$(Build.ArtifactStagingDirectory)/**/*.nupkg;!$(Build.ArtifactStagingDirectory)/**/*.symbols.nupkg'
    nuGetFeedType: 'external'
    publishFeedCredentials: 'nuget'
